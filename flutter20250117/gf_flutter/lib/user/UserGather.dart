
import 'package:flutter/material.dart';

class UserGather extends StatelessWidget {
  const UserGather({super.key});

  @override
  Widget build(BuildContext context) {
    return   Center(child: Text('Welcome to the User Gather page!'),
    );
  }
}